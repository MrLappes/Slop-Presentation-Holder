services:
  slop:
    build: .
    ports:
      - "6080:6080"
    volumes:
      - audio_cache:/app/audio_cache
      - voices:/app/voices
      - ./projects:/app/projects
    restart: unless-stopped

volumes:
  audio_cache:
  voices:
