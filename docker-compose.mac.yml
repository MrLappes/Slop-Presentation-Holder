services:
  slop:
    environment:
      DISPLAY: ${DISPLAY:-host.docker.internal:0}
      QT_QPA_PLATFORM: xcb
      QT_X11_NO_MITSHM: "1"
      PULSE_SERVER: ${PULSE_SERVER:-host.docker.internal:4713}
    extra_hosts:
      - host.docker.internal:host-gateway
